# MLB First At-Bat Pipeline — Python dependencies
# Install with: pip install -r requirements.txt

pybaseball>=2.2.7       # Baseball Savant + FanGraphs + Baseball Reference scraper
pandas>=2.0.0
numpy>=1.24.0
requests>=2.28.0
python-dotenv>=1.0.0
tqdm>=4.65.0
pyarrow>=12.0.0         # Parquet support
lxml>=4.9.0             # HTML parsing
beautifulsoup4>=4.12.0

# Optional — for alerts
twilio>=8.0.0           # SMS alerts (pip install twilio)

# Optional — for model training (next phase)
scikit-learn>=1.3.0
xgboost>=2.0.0
lightgbm>=4.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
shap>=0.43.0            # Feature importance / explainability
jupyter>=1.0.0          # For exploratory analysis
